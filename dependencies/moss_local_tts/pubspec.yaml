name: moss_local_tts
description: Android on-device MOSS-TTS-Nano bridge for Kelivo.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.psyche.moss_local_tts
        pluginClass: MossLocalTtsPlugin
