# fast arm64-only validation lane for Story Runtime and Composer integration
name: Story Fast Debug

on:
  push:
    branches:
      - feat/story-runtime-foundation
    paths:
      - .github/workflows/story-fast-debug.yml
  workflow_dispatch:

permissions:
  contents: write

env:
  FLUTTER_VERSION: '3.44.6'

jobs:
  android-debug:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout exact triggering commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Use hosted JDK 17
        shell: bash
        run: |
          set -euo pipefail
          echo "JAVA_HOME=$JAVA_HOME_17_X64" >> "$GITHUB_ENV"
          echo "$JAVA_HOME_17_X64/bin" >> "$GITHUB_PATH"
          "$JAVA_HOME_17_X64/bin/java" -version

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Inject fallback
        shell: bash
        run: |
          mkdir -p lib/secrets
          cat > lib/secrets/fallback.dart <<'EOF'
          const String siliconflowFallbackKey = '${{ secrets.SILICONFLOW_KEY }}';
          EOF

      - name: Resolve dependencies
        run: flutter pub get

      - name: Static analysis
        run: dart analyze --fatal-infos lib test

      - name: Focused Skill lifecycle regression
        run: |
          flutter test --concurrency=1 \
            test/features/story_runtime/story_skill_github_install_dialog_test.dart \
            test/features/story_runtime/built_in_story_skill_assets_test.dart \
            test/features/story_runtime/story_conversation_mode_control_test.dart

      - name: Build arm64 Android debug APK
        run: flutter build apk --debug --target-platform android-arm64

      - name: Verify debug APK signature
        shell: bash
        run: |
          set -euo pipefail
          apksigner="$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -n 1)"
          test -n "$apksigner"
          "$apksigner" verify --verbose build/app/outputs/flutter-apk/app-debug.apk

      - name: Package
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(grep '^version:' pubspec.yaml | sed 's/version:[[:space:]]*//' | tr -d ' ')"
          SHORT_SHA="$(git rev-parse --short=8 HEAD)"
          mkdir -p dist
          cp build/app/outputs/flutter-apk/app-debug.apk "dist/Kelivo_${VERSION}_Story_TEST_${SHORT_SHA}_arm64-v8a-debug.apk"
          sha256sum dist/*.apk > dist/SHA256SUMS.txt
          printf 'commit=%s\nversion=%s\nabi=arm64-v8a\nsigning=android-debug\nvalidation=static-analysis+focused-skill-regression+apk-signature\n' \
            "$(git rev-parse HEAD)" "$VERSION" > dist/BUILD_INFO.txt

      - name: Publish prerelease
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          VERSION="$(grep '^version:' pubspec.yaml | sed 's/version:[[:space:]]*//' | tr -d ' ')"
          VERSION_NAME="${VERSION%%+*}"
          SHORT_SHA="$(git rev-parse --short=8 HEAD)"
          TAG="story-test-v${VERSION_NAME}-${SHORT_SHA}"
          gh release create "$TAG" dist/* \
            --repo "$GITHUB_REPOSITORY" \
            --target "$(git rev-parse HEAD)" \
            --title "Kelivo Story Runtime TEST ${VERSION} (${SHORT_SHA})" \
            --notes "arm64-v8a Android debug-signed validation build for Story Runtime and Composer integration. Static analysis, focused Story regressions and APK signature verification passed before publishing. Full non-baseline validation continues independently." \
            --prerelease

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: story-fast-debug-arm64
          path: dist/
          if-no-files-found: error
          retention-days: 14
